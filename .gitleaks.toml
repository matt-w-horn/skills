# gitleaks config — keeps every built-in secret rule. The skills hold no
# credentials by design (prompts, reference docs, and stdlib Python), so this
# is a backstop against an accidental paste.
# https://github.com/gitleaks/gitleaks#configuration
title = "skills"

[extend]
useDefault = true # built-in rules: API keys, generic high-entropy, etc.
